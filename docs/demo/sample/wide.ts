export function formatUserSummary(firstName: string, lastName: string, visitCount: number): string {
	return "User " + firstName + " " + lastName + " visited us " + visitCount + " times and we are always happy to see them again";
}
